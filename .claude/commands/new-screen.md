Create a new screen based on the INTERFACE_SPEC.md specification.

Instructions:
1. Read INTERFACE_SPEC.md to find the spec for the requested screen: $ARGUMENTS
2. Create the screen file in the correct location under `app/` following Expo Router file-based routing conventions
3. Apply the design system from INTERFACE_SPEC.md — use the correct color tokens, typography, spacing, and interaction rules
4. Follow the "No-Line Rule" — no 1px solid borders, use background shifts and tonal transitions
5. Use warm parchment palette (#FCF9F2 base), never cold white
6. Use functional components with TypeScript
7. Extract reusable components into `components/` if they will be shared across screens
