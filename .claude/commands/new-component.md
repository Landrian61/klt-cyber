Create a new reusable component for the KLT Cyber Church app.

Instructions:
1. Read INTERFACE_SPEC.md to understand the design system and find any spec for: $ARGUMENTS
2. Create the component in `components/` (or `components/ui/` if it's a low-level primitive)
3. Use TypeScript with proper prop types (interface, not type alias)
4. Use functional component with named export
5. Apply the Sacred Curator design language — warm parchment palette, no hard borders, gold accents
6. Include all relevant states (default, pressed, disabled, loading) as defined in the spec
7. Use React Native Reanimated for any animations
