Set up or update the theme system based on INTERFACE_SPEC.md.

Instructions:
1. Read INTERFACE_SPEC.md Section 1 (Design System) completely
2. Update `constants/theme.ts` with all color tokens, typography scales, spacing values, and elevation styles from the spec
3. Ensure the theme object is fully typed with TypeScript
4. Include both the color palette and semantic tokens (success, error, warning, live, unread)
5. Include the Glass & Gold surface styles
6. Make sure all values match INTERFACE_SPEC.md exactly
